# Changelog

All notable experiments and infrastructure changes in this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
with weekly groupings corresponding to experimental campaigns.

---

# Backlog

Priority colours: 🔴🟠🟡🟢

## Partially Completed Reports

Reports that have been started but are not yet fully complete (i.e., not all pipeline steps finished: plan report, implement code, review implementation, generate raw data and figures, write final report).

- 🟡 **High-Order Squeezing Plan** (#20260507) — Hypothesis and theoretical model written. Next step: implement code (build the bosonic+spin oscillator simulation, Lindblad master equation solver with order-$n$ spin-dependent forces).

- 🟡 **Advanced Architecture Surveys** (#20260511) — Theoretical framework documented for six models (non-Markovian bath, thermal Langevin noise, cavity-enhanced MZI, distributed sensor arrays, dynamical decoupling, tilt-to-length noise). Next step: implement code (build model-specific simulators and figure generators).

## Experiments

- 🟠 **Free ancilla with joint measurement** — Apply the free-ancilla initial state (20260528) to the weighted joint measurement protocol (20260518 NM generalization) to test whether a free ancilla unlocks better joint-readout sensitivity than a fixed $|1,0\rangle$ ancilla. See #20260518, #20260528.

- 🔴 **Heisenberg-limited MZI with squeezed-vacuum and OAT** — Extend the CFI analysis (#20260601) to squeezed-vacuum input and OAT-generated spin-squeezed states; confirm $\alpha\to-1.0$ and compare prefactor with NOON/Twin-Fock. See #20260601.

## Infrastructure & Tooling

- 🔴 **3D slice visualization** — Heatmap infrastructure currently supports 2D slices only. For studying all three drive components simultaneously, 3D volumetric plots or 2D projections of 3D landscapes are needed.

## Week 26 (Jun 22–28)

### Infrastructure

- **Module extraction & codebase simplification** — Split 3 large modules into 12 smaller modules: ancilla_drive_metrology (1596→3), ancilla_optimization (2693→3), scaling_survey (3971→6). Created pipeline abstractions (optimisation_pipeline, checkpoint_recovery, slice_scan) and promoted 8 duplicated helpers to shared `src/` modules, saving ~2000–2300 LOC. Extracted survey_models.py (585 LOC) to bring scaling_survey.py below 1500. Refactored weak_value_mzi under length limits. Reduced Radon CC on 5 functions (CC 10–12 → 2–7). Applied 17 audit-remediation fixes across #20260616 and #20260619. Restructured _report_local convention to eliminate F401 re-export vulnerability.

- **Performance optimization** — Achieved 6–19× speedup on evolve_mzi (21,969 calls, 34.4s cumulative) via @lru_cache on beam_splitter_unitary and system_ancilla_interaction_unitary, plus an element-wise fast path for ancilla_dim==1 that avoids np.kron. Cached create_system_operators with @lru_cache(maxsize=32). All tests pass post-optimization.

- **Test coverage expansion** — Added 389 new unit tests across 4 modules: survey-analysis modules (136 tests, 5 modules, all ≥93% coverage), single_particle_mzi (108 tests, 100%), multi_mzi (68 tests, 100%), and hybrid_mzi (77 tests, 100%). Fixed lindblad_rhs test collection error in reports/20260507.

- **Code quality & tooling infrastructure** — 17 lint/type fixes across 7 Ruff rules (TC003, PT006, PT019, RUF059, F841, F821) and 3 Mypy categories (12 missing annotations, abstract instantiation, incompatible types). Tightened CI lint rule for module-level constants in src/. Added jscpd code-duplication baseline test (src/ 0.52%, reports/ 5.92%). Project health alignment (27 items), T_hold→t_hold rename (~1200 occurrences), ParquetSerializable ABC on 47 dataclasses, 87% coverage gate, CI gate for TODO/FIXME/placeholders, noqa suppression survey (44/98 eliminated), Radon CC campaign cleared all C-grade blocks.

- **Engineering studies** — Evaluated Semgrep (reject — existing toolchain sufficient), Pydantic (reject — 20–30h migration cost, no benefit for numpy-heavy patterns), Hypothesis (adopt — 9 property-based pilots, 0.4% overhead, max_examples=100, max_photons=[1,5]), and Vulture (reject as CI gate — adopt as periodic manual scan). See per-study entries for detailed recommendations.

- **Analytical derivation of $(a_z, a_{zz})$ SQL flatness** (#20260527) — Derived the exact SQL-level sensitivity of the commuting $(a_z, a_{zz})$ slice analytically. The proof shows that $[J_z^A, H] = 0$ implies $J_z^A$ is conserved, the $a_z$ drive contributes only a global phase, and the Ising interaction reduces to a static frequency shift $\omega \to \omega + a_{zz}/2$, making the protocol unitarily equivalent to a single-spin Ramsey interferometer. Full derivation added to the report as a new ⚖️ Analytical Bounds section. See #20260527.

- **Pyright pre-existing errors (7)** — Fixed 7 pyright errors across 3 files: `src/analysis/scaling_survey.py` (complex→float via `.real`), `src/physics/hybrid_mzi.py` (`.T` on tuple via `type: ignore[attr-defined]`), `src/physics/test_pseudomode_system.py` (`.imag` on ndarray via `np.imag`). Pyright now clean on all 3 files.

- **Project alignment review** — Backlog priorities reviewed (9 items, 2🔴/2🟠/3🟡/2🟢), all 25 report directories cross-referenced against CHANGELOG entries (none missing), toolchain health verified (ruff: clean, mypy: 0 errors, pytest: 3587/3587 pass, coverage: 88%), cross-report duplication analysis completed (8 promotion opportunities identified), and stale file scan (no runners, no orphaned dirs). Merged duplicate "Advanced architecture simulation functions" into #20260511; added 3 new backlog items (pyright errors, xfailed test resolution, promotion opportunities).

- **Cross-report function promotion** — Promoted 6 duplicated report-level functions to `src/`, resolving the `test_functions_duplicated_across_reports` xfail (now passes). Changes: (1) `compute_drive_sensitivity_with_details` added to `src/analysis/ancilla_drive_metrology.py`; (2) `compute_sensitivity` and `compute_decoupled_baseline` replaced in `reports/20260621/bell_state_initial_entanglement.py` with shared versions; (3) `compute_decoupled_baseline` replaced in `reports/20260620/multi_particle_free_ancilla.py` with `sql_reference(N, T_HOLD)`; (4) `build_decoupled_baseline_df` added to `src/analysis/decoupled_baseline.py` — back-ported to `reports/20260611`, `20260612`, `20260616`; (5) `generate_n_scaling` renamed to `run_n_scaling_sweep` in `reports/20260518/ancilla_drive_enhanced_metrology.py`. Updated duplication baseline (src/: 0.52→0.61%). Removed xfail marker from `test_functions_duplicated_across_reports`. All 1391 tests pass.

- **Cross-report promotion campaign: three more candidates promoted** — (1) `_build_rs_result`/`_build_nm_result` promoted to `src/analysis/optimisation_pipeline.py` as `build_rs_result`/`build_nm_result`, back-ported to reports/20260519, 20260611, 20260612. (2) `plot_ratio_heatmap`/`plot_alpha_opt_heatmap` promoted to `src/visualization/coupling_heatmaps.py` with flat-array interface, back-ported to reports/20260522, 20260523, 20260525. (3) `run_n_scaling_scan` orchestrator and helpers (`build_n_result_from_row`, `build_n_result_from_dict`, `n_group_key`, `make_checkpoint_name_fn`) added to `src/analysis/n_scaling_sweep.py`, back-ported to reports/20260611, 20260612 removing ~120 LOC from each. All 3580 tests pass. Duplication baseline dropped from 5.92% to 5.31%.

- **Final two cross-report promotions completed** — All 8 promotion candidates now resolved. (1) **Free-Ancilla Evolving Model**: `compute_free_ancilla_sensitivity(evolve_fn, ...)` added to `src/analysis/ancilla_drive_metrology.py` with pluggable evolution-engine callback. Back-ported to `reports/20260528/free_ancilla_initial_state.py` and `reports/20260610/free_ancilla_omega_modulated.py`. 3 new tests. (2) **XX Coupling Sweep Pipeline**: `run_sweep_base`, `resolve_sweep_defaults`, and `_store_result_values` added to `src/analysis/coupling_sweep.py`. Back-ported to 3 reports: `reports/20260522/multi_particle_xx_coupling.py`, `reports/20260523/four_param_coupling_multi_particle.py`, `reports/20260525/joint_measurement_xx_coupling.py`. 12 new tests. The sweep pattern is now orchestrated by `run_sweep_base` which handles array allocation, row-major (N-outer, ω-inner) iteration, per-point N-dependent operator caching, and automatic ratio computation. All 3623 tests pass. See `# Backlog > Infrastructure & Tooling`.

- **Missing libcudnn.so.9** (#20260518) — `torch._C` failed at import time because the `nvidia-cudnn-cu11` dependency in `pyproject.toml` conflicted with the `nvidia-cudnn-cu13` package required by torch 2.12.0 (both write to `nvidia/cudnn/`). Removed stale `nvidia-cudnn-cu11` dependency and copied missing `.so` files from uv's cached wheel archives into the venv. All 218 report tests and 3607 main suite tests pass. See `# Backlog > Infrastructure & Tooling`.

---

## Week 25 (Jun 15–21)

### New Report
- **Non-Linear Measurement (Parity and CFI) on ω-Modulated Drive** (#20260615) — Replaces the linear $J_z^S$ measurement in $\omega$-modulated drive (#20260519) with parity and full-distribution CFI strategies. CFI consistently beats linear $J_z^S$ ($\mathcal{R}_{\text{CFI}} \to 1.4$ at $N=10$). Parity beats linear when optimised for parity (8–34%) but shows no Heisenberg scaling. Even optimal S-only CFI captures only $\sim 44–47\%$ of total Fisher information vs joint measurement. See #20260519, #20260601, #20260613.
- **General 4-Parameter ω-Modulated Drive** (#20260616) — Full report compiled. The combined protocol achieves $12\%$ improvement over the Ising-only drive at $N=1$ ($\Delta\omega = 0.01814$, $\mathcal{R}=5.51$ at $\omega=0.7$). However, the central $N$-scaling hypotheses fail: scaling exponents are uniformly positive ($\alpha \approx 0.09$–$0.34$) for both $J_A=1/2$ and $J_A=N/2$ — sensitivity *degrades* with $N$. The 7D optimisation landscape becomes intractable at $N>8$ despite 5000 random samples + 200 L-BFGS-B refinements. Of 10 success criteria, 7 pass (decoupled baseline, $N=1$ consistency, $N=1$ improvement, non-commuting drive, finite $\alpha$, numerical validity, Parquet roundtrip) and 3 fail ($N>1$ ratio improvement, $F_Q \propto N^2$ scaling, scaling analysis). All 11 figures and 7 Parquet files verified. Conclusions discuss optimisation complexity as the primary barrier. See #20260519, #20260521, #20260611.
- **Ancilla OAT Pre-Squeezing before $\omega$-Modulated Hold** (#20260619) — Full report compiled. OAT pre-squeezing ($U_{\text{OAT}} = \exp(-i q (J_z^A)^2)$) added to the multi-particle ancilla protocol (#20260612) with CSS ancilla initial state. Phase 1 ($N=1$, $J_A=1$): OAT improves sensitivity at all $\omega$ (best $\Delta\omega=0.0161$, $R=6.21$, $\mathcal{I}=1.0$–$4.1$). Phase 2 ($N=2$–$6$, $J_A=N/2$): all beat SQL ($R=1.4$–$3.8$), OAT active in 97% of cases. Scaling exponent $\alpha\approx-0.18$ (worse than SQL $-0.5$) — claim 3 rejected. 7/10 success criteria PASS, 1 PARTIAL, 1 FAIL. OAT-CSS protocol is intermediate between $\theta$-drive (#20260612) and $\omega$-drive (#20260616). 2 Parquet files, 8 SVG figures, 197 tests. See #20260519, #20260611, #20260612, #20260613.
- **Multi-Particle Ancilla ($J_A > 1/2$) with Free Initial State and $\omega$-Independent Drive** (#20260620) — Full report compiled. Tests whether a multi-particle ancilla ($M=2,3,4$, $J_A=1,3/2,2$) with free initial state $(\theta_A,\phi_A)$ and $\omega$-independent drive can circumvent the SQL bound that was absolute at $J_A=1/2$ (#20260528). System size $N$ varies independently ($N=1$–$10$). 4 dataclasses (`FreeAncillaNScalingResult`, `FreeAncillaNScalingScanResult`, `FreeAncillaRandomSearchResult`, `FreeAncillaNelderMeadResult`). Results: null hypothesis confirmed — $\omega$-independent drive cannot beat SQL for ANY ($N=1$–$10$, $M=1$–$4$, $\omega=0.1$–$5$) configuration. All 80 directly-verified triples give exactly $\Delta\omega = 1/(\sqrt{N}T_H)$ (ratio $R=1.0$). Scaling exponent $\alpha=-0.5$ (SQL scaling). $\omega$-modulation of ancilla drive is identified as the essential mechanism for SQL violation. 104 tests pass, 2 Parquet files, 15 SVG figures. See #20260528, #20260612.
- **Bell-State Initial S--A Entanglement in Driven-Ancilla Metrology** (#20260621) — Full results at 50-point ω resolution. Null hypothesis confirmed: Bell state $|\Phi^+\rangle$ does **not** circumvent the $J=1/2$ bound for $\omega$-independent drive. All 150 finite-Δω results (Scenarios A, B, D) give exactly $\Delta\omega = 0.100000 = \text{SQL}$ at every ω (ratio $R=1.0000$). Scenario C (Bell + drive only) is analytically fringe (infinite sensitivity) — the Bell state's reduced system density matrix is maximally mixed at zero interaction. Decoupled baseline passes all 200 verifications. Optimal $t_{\text{hold}}$ always saturates its upper bound (10.0), and optimal drive/interaction parameters vary widely across ω but all converge to same SQL-level sensitivity. The $J=1/2$ spectral radius bound is absolute for $\omega$-independent drives regardless of initial entanglement. This closes the question: $\omega$-modulation of the ancilla drive (demonstrated in #20260519, #20260610, #20260611, #20260612, #20260613) remains the only demonstrated SQL-violation mechanism. 684s serial runtime for 200 pairs. 2 Parquet files, 14 SVG figures, 69/69 tests pass. See #20260528, #20260519, #20260620.

## Week 24 (Jun 8–14)

### New Report
- **Multi-Particle $\omega$-Modulated Ancilla Drive ($J_A = N/2$)** (#20260612) — Extends the $\omega$-modulated ancilla drive (#20260519, #20260611) to a multi-particle ancilla with $M=N$ ($J_A=N/2$). SQL violation confirmed at all N=1–8. The ratio arrests decay ($R_{\text{multi}}(8)=4.68$ vs $R_{\text{fixed}}(8)\approx1.3$, 3.6× improvement) but the N-scaling exponent stays at SQL ($\alpha\approx-0.5$). N=9–20 scan still pending. **5 figures generated**: ratio vs N, sensitivity vs N (log-log), optimal params vs N, comparison with fixed ancilla, and variance/derivative diagnostic — all from N=1–8 checkpoint data. See #20260519, #20260611.
- **N-Scaling of the Phase-Modulated Ancilla Drive** (#20260611) — Extends the $\omega$-modulated ancilla drive (#20260519) to $N>1$ system particles ($J_S=N/2$, $J_A=1/2$). SQL violation persists at all $N\in[1,20]$ (100/100 pairs beat SQL) but the ratio decays $R(N)\to 1$ as $N$ grows ($R(1)=4.91$, $R(20)\approx1.04-1.21$), confirming $O(1/N)$ suppression of the $J_A=1/2$ ancilla contribution. Scaling exponents $\alpha\in[-0.24,-0.05]$ far below SQL ($-0.5$). See #20260519.
- **Free Ancilla with $\omega$-Modulated Drive** (#20260610) — Combines the free-ancilla initial state (#20260528) with the $\omega$-modulated drive (#20260519). Primary hypothesis confirmed: best $\Delta\omega=0.01364$ at $\omega=0.1$ ($7.3\times$ SQL, $1.49\times$ better than fixed-ancilla baseline). $\theta_A^*(\omega)$ varies systematically ($2.04$ at $\omega=0.1$ to $0$ at $\omega=5.0$), confirming secondary hypothesis. See #20260519, #20260528.
- **$\omega$-Modulated Drive with Weighted Joint Measurement** (#20260613) — Combines the $\omega$-modulated drive (#20260519) with weighted joint measurement $M = \cos\psi\,J_z^S + \sin\psi\,J_z^A$ (#20260525). Both hypotheses confirmed. Step 1 ($N=1$): Joint beats S-only by 46--220%. Best $\Delta\omega=0.01027$ ($9.7\times$ SQL, $2.5\times$ S-only best). Optimal $\psi^*\approx1.3-1.8$ rad gives $\sim 98\%$ ancilla weight. Step 2 ($N>1$): Joint arrests the $R(N)\to1$ decay — $R(20)\approx1.7-1.9$ vs $R(20)\approx1.0$ for S-only. Scaling exponent $\alpha\in[-0.06,-0.02]$, flatter than SQL's $-0.5$. See #20260519, #20260525, #20260611.
- **Non-Markovian Bath with Ancilla Protection** (#20260509) — Full pipeline: tripartite pseudomode simulator (src/physics/pseudomode_system.py, 61 tests), sweep infrastructure (reports/20260509/non_markovian_ancilla.py, 39 tests), implementation audit, and parameter sweeps. All 100 tests pass. Ancilla sweep (51 pts, $\theta\in[0,\pi]$): $\theta^*=\pi/2$, $\mathcal{R}_{\text{with}}=4.37$ ($3.2\times$ no-ancilla). Memory sweep (40 pts, $\lambda\in[0.05,10]$): $\Delta\mathcal{R}=3.97$ at $\lambda=0.05$ vs $0.86$ at $\lambda=10$ — non-Markovian advantage confirmed. Time sweeps (4×31 pts, $T\in[0,5]$): $\mathcal{R}=12.6$ at $\theta=\pi/2$, $T=5$, demonstrating QFI enhancement ($\mathcal{R}>1$) via non-Markovian information backflow amplified by the ancilla. QuTiP sparse Liouvillian solver added for performance. 6 Parquet files, 3 SVG figures, report updated with embedded results.

### Infrastructure
- **Code migration: 21 duplicated items promoted to shared `src/` modules** (#20260611, #20260612) — 21 duplicated items between #20260611 and #20260612 consolidated into 4 shared modules: `src/utils/parallel.py`, `src/analysis/sensitivity_metrics.py`, `src/analysis/n_scaling_result.py`, `src/visualization/scaling_plots.py`. `parallel_map` back-ported to 4 older reports. 15 new tests. See `.opencode/skills/audit-code/SKILL.md`.
- **Symbol disambiguation campaign** — Renamed five overloaded symbols: phase rate `θ`→`ω`; bare `T`→semantic names (`T_hold`, `T_evo`, etc.); BS reflection `φ`→`phi_bs`, MZI estimation `φ`→`phi_phase`, Bloch azimuth `φ`→`phi_Bloch`; Kerr `χ`→`K`. Removed stale backlog item.
- **API hardening and infrastructure cleanup** — Made `basis` keyword-only on `jz/jx/jy_operator` (~80 sites). Removed `validate_state`/`validate_hamiltonian` aliases; replaced 5 local Pauli redefinitions with `src.utils.constants`; renamed `delta`→`fd_step`, `hold_unitary`→semantic names, `N_traj`→`n_traj`, `N_points`→`n_points`. Fixed 8 test regressions.
- **Stratified norm-ball sampling resolves small-$r$ envelope** (#20260527) — Ran stratified norm-ball sampling (50 $\omega$ × 5000 samples, 50 radial strata) for report #20260527. Stratified sampling provides 2492$\times$ more samples at $\|\mathbf{a}\|\le 0.1$ than Marsaglia's method. The envelope is flat at SQL (ratio = 1.0) for all resolved $r\in[0.1,10.0]$, rejecting the hypothesis that small drive amplitudes meaningfully degrade sensitivity. The apparent small-$r$ degradation in the original Marsaglia data was a sampling artifact. Report updated with Experiment 2b (stratified results), new envelope figure, and corrected conclusions. 2 new Parquet files (samples + envelope), 1 new SVG. See #20260608.

## Week 23 (Jun 1–7)

### New Report
- **Heisenberg-Limit MZI with NOON and Twin-Fock States** (#20260601) — CFI vs error-propagation in the standard MZI shows $\langle J_z\rangle_{\text{out}} = 0$ degeneracy for NOON $N\ge 2$. CFI from the full $P(m|\theta)$ distribution resolves the degeneracy. Both NOON ($F_C = H_t^2 N^2$, $\alpha=-1.0$) and Twin-Fock ($F_C = H_t^2 N(N+2)/2$, $\alpha=-0.907$) saturate their QFI bounds. CFI is $\theta$-independent for both states. Number-difference measurement is optimal in the balanced MZI.

### Infrastructure
- **Stratified Monte Carlo sampler** (#20260608) — New `src/utils/monte_carlo.py` module providing `stratified_ball_sample()` (uniform in radius, uniform direction on 2-sphere) and `marsaglia_ball_sample()` (promoted from report #20260527). Stratified method divides $[0,R]$ into equal-width radial strata, each receiving equal sample count — resolving the small-$\|\mathbf{a}\|$ regime that Marsaglia's uniform-volume method under-samples. Integrated into `reports/20260527/drive_component_analysis.py` via a `sampling_method` parameter (`"marsaglia"` default, `"stratified"` alternative) with CLI `--method` flag. 31 new tests across `src/utils/test_monte_carlo.py` and `reports/20260527/test_drive_component_analysis.py`.
- **CHANGELOG.md** — Created CHANGELOG.md with weekly groupings (Keep a Changelog format). Backlog of 19 items with priority emojis and `Next step:` pipeline annotations. Partially completed reports tracked separately. Updated all 4 skill files and research-assistant.md to reference the CHANGELOG and removed stale `## [Unreleased]` references. Consolidated "New Report" / "Report Variation" categories, removed monthly section headers, and lowered all backlog priorities by one level.

## Week 22 (May 25–31)

### New Report
- **Free Ancilla Initial State in Driven-Ancilla Metrology** (#20260528) — Freeing the ancilla to $(\theta_A,\phi_A)$ does not enable SQL violation. All four scenarios (A: fixed-ancilla baseline, B: free+drive+interaction, C: free+interaction-only, D: free+drive-only) converge to exactly $\Delta\theta=0.1$ at all 5 $\theta$ values. The $(\theta_A,a_{zz})$ slice is flat at SQL across 40,401 grid points. The $J=1/2$ bound is absolute for product initial states. See #20260518, #20260519, #20260521.
- **Drive-Component Analysis in Ancilla-Enhanced Metrology** (#20260527) — Systematic 2D slice analysis of $(a_x,a_{zz})$, $(a_y,a_{zz})$, and $(a_z,a_{zz})$ at 5 $\theta$ values each. All three achieve min ratio $=1.0$ (SQL). The commuting $(a_z,a_{zz})$ slice is qualitatively different: 0% of valid points show degradation, vs 78--87% for non-commuting $(a_x,a_{zz})$ and $(a_y,a_{zz})$. Norm-ball envelope: stratified sampling reveals the envelope is flat at 1.0 (SQL) for all resolved $r$, rejecting earlier concern that small drive degrades sensitivity. Barycentric heatmaps confirm yellow (R+G) dominance. See #20260519, #20260521.

### Infrastructure
- **Norm-ball Monte Carlo sampler** — Marsaglia 3-ball sampling for $\|\mathbf{a}\|\le R$ with envelope curve extraction, validated via Kolmogorov--Smirnov test against $(r/R)^3$ CDF.
- **Barycentric sensitivity heatmap** — Combined RGB visualization that encodes each drive component's $\log_{10}(\Delta\theta/\text{SQL})$ contribution into separate colour channels.
- **Free ancilla state preparation** — Generalized initial state $|\psi_A\rangle = \cos(\theta_A/2)|1,0\rangle + e^{i\phi_A}\sin(\theta_A/2)|0,1\rangle$ as a 4D complex vector, accepted by the existing circuit evolution pipeline.
- **Cross-scenario comparison framework** — Four-scenario dispatcher with common random-search and Nelder--Mead refinement pipeline, norm-envelope overlay, and summary statistics.
- **2D slice $(\theta_A, a_{zz})$** — Generic 201$\times$201 grid scan supporting any controllable parameter against $a_{zz}$, enabling comparisons with existing $(a_x,a_{zz})$, $(a_y,a_{zz})$, $(a_z,a_{zz})$ slices.

## Week 21 (May 18–24)

### New Report
- **Multi-Particle XX-Coupling Dual-MZI with Optimized Joint Measurement** (#20260525) — Joint measurement $M = m_s J_z^S + m_a J_z^A$ with $m_s^2+m_a^2=1$, jointly optimized with $\alpha_{xx}$. Best ratio $\Delta\theta/\text{SQL}_{2N}=0.509$ at $N=5$, $\theta=0.5$. Asymptotic scaling $\Delta\theta \propto 1/N^{0.75}$ (between SQL $\alpha=-0.5$ and HL $\alpha=-1.0$). Optimal weights are always unequal ($m_s\neq m_a$), with ancilla weight $m_a$ dominating at large $\alpha_{xx}$. See #20260522, #20260518.
- **Phase-Diffusion Robustness of the Phase-Modulated Drive** (#20260524) — Sub-SQL advantage of the phase-modulated drive protocol survives phase diffusion up to $\gamma_\phi^*\approx0.08$ at $\theta=0.2$ and $\gamma_\phi^*\approx0.12$ at $\theta=1.0$. Optimal drive amplitudes $(a_x,a_y,a_z)$ decrease with $\gamma_\phi$ while $\alpha_{zz}$ increases to compensate. See #20260519.
- **Four-Parameter Coupling Multi-Particle Dual-MZI** (#20260523) — Full $(\alpha_{xx},\alpha_{xz},\alpha_{zx},\alpha_{zz})$ interaction with $N\in[1,10]$ and dual MZI. Weak SQL violation: best ratio $\Delta\theta/\text{SQL}=0.916$. Dual MZI symmetrization weakens the static cross-term generation that worked at $N=1$. See #20260522.
- **Multi-Particle XX-Coupling Dual-MZI** (#20260522) — $N\in[1,20]$ particles in both S and A, dual MZI (BS on both before and after hold), $H_{\text{int}} = \alpha_{xx} J_x^S \otimes J_x^A$ only, S-only measurement after trace. No SQL violation at any $N$: $\alpha_{xx}^*=0$ always. Dual MZI does not unlock XX-coupling advantage. See #20260520.
- **General-Interaction Ancilla Metrology** (#20260521) — Symmetric encoding with full four-parameter interaction $(\alpha_{xx},\alpha_{xz},\alpha_{zx},\alpha_{zz})$ and L-BFGS-B optimization. Best $\Delta\theta = 0.0690$ ($0.690\times$ SQL) at $\theta=3.8$. Dominant couplings are $\alpha_{zx}$ and $\alpha_{zz}$, generating SQL violation through higher-order BCH cross-terms with $H_0 = \theta(J_z^S + J_z^A)$. See #20260518, #20260520.
- **XX-Coupling Ancilla Metrology** (#20260520) — Symmetric phase encoding ($H_A = \theta J_z^A$) with transverse coupling $H_{\text{int}} = \alpha_{xx} J_x^S \otimes J_x^A$ and S-only measurement after trace. No SQL violation: $\alpha_{xx}=0$ always optimal for all $\theta$ and $\alpha_{xx}\in[0,20]$. XX coupling creates transverse entanglement that is destroyed by the trace.
- **Ancilla-Drive Phase-Modulated Metrology** (#20260519) — Making the ancilla drive $\theta$-dependent ($H_A = \theta\cdot\mathbf{a}\cdot\mathbf{J}^A$) unlocks SQL violation: best $\Delta\theta = 0.02036$, which is $4.91\times$ below the SQL at $\theta\approx0.2$. Mechanism: static BCH cross-term $[\theta J_z^S,\, a_{zz} J_z^S\otimes J_z^A]$ generates effective $J_z^A$ contribution. Violation requires both $a_{zz}\neq0$ and small $\theta$. See #20260518.
- **Ancilla-Drive-Enhanced Metrology** (#20260518) — Active ancilla drive $H_A = a_xJ_x^A + a_yJ_y^A + a_zJ_z^A$ with Ising interaction $H_{\text{int}} = a_{zz}J_z^S\otimes J_z^A$. No SQL violation across 2500 4D random-search evaluations, 10 complete 2D slice grids, or 250 Nelder--Mead refinements. The $J=1/2$ spectral radius bound is absolute for $\theta$-independent ancilla drive. See #20260515.
- **Weighted Joint Measurement: N-M Generalization** (#20260518) — Joint measurement $M = aJ_z^S + bJ_z^A$ with $a^2+b^2=1$ and CSS initial states at $N$ system and $M$ ancilla particles. With free interaction coefficients, the optimiser turns the interaction off ($\nu=0.085$, 95% CI $[-0.27,0.24]$, far below SQL $\nu=0.5$). With constrained interaction ($\alpha_{xx}\neq0$), weighted measurement beats S-only up to $85\%$ at $\alpha_{xx}=-1.8$. See #20260515.

## Week 20 (May 11–17)

### New Report
- **Ancilla-Assisted Metrology with Joint Measurement** (#20260515) — Joint measurement $M = J_z^S + J_z^A$ at $\alpha\neq0$ recovers sensitivity that S-only measurement loses, but never beats the SQL ($\Delta\theta = 1/T_H$ at best). QFI bound $F_Q \leq T_H^2$ is fundamental. See #20260512.
- **Ancilla-Assisted Metrology: Full-Parameter Optimization** (#20260512) — Nelder--Mead optimization over all four bilinear interaction coefficients $(\alpha_{xx},\alpha_{xz},\alpha_{zx},\alpha_{zz})$ in a two-qubit ancilla-assisted MZI. Optimal configuration is always $\alpha_{ij}=0$, recovering the SQL. Non-zero $\alpha_{ij}$ generally degrades sensitivity when measuring $J_z^S$ alone. See #20260511.
- **Single-Particle MZI Holding-Time Scaling** (#20260512) — Baseline reference: $\Delta\theta = 1/T_H$ (SQL) exactly for $T_H\in[10^{-2},10^3]$, log-log scaling exponent $\alpha=-1.0$. Analytical and finite-difference derivatives agree to machine precision. At fringe extrema ($\sin(\theta T_H)=0$) the sensitivity diverges as expected.
- **Ancilla-Assisted vs Two-System-Particle Comparison** (#20260511) — Fixed 2 total particles: ancilla-assisted configuration (1 S + 1 A) cannot beat two-system-particle configuration (2 S). Ratio $\mathcal{R} = \Delta\theta_A / \Delta\theta_B = 2.02$. Fundamental limitation: $J=1/2$ probe cannot behave like a $J=1$ probe.
- **Scaling Survey: Collective-Spin / Dicke Basis** (#20260511) — OAT spin squeezing achieves $\alpha=-2/3$ at optimal $t_{\text{opt}}\propto N^{-1/3}$. Balanced Dicke (Twin-Fock) achieves $\alpha=-1.0$ (Heisenberg) with $F_Q = N(N+2)/3$. Non-Gaussian states have fractional exponents $\alpha\approx-0.75$ to $-0.85$.
- **Scaling Survey: Fock-Basis MZI Models** (#20260511) — Systematic scaling analysis of coherent, NOON, and squeezed-vacuum states under Markovian decoherence. Coherent states achieve SQL ($\alpha=-0.5$). Squeezed-vacuum achieves Heisenberg-like scaling ($\alpha\to-1.0$) with $F_Q = 2\langle N\rangle(\langle N\rangle+1)$, beating the NOON-state HL prefactor by $2\times$. NOON collapses to SQL under any loss; two-body loss causes complete collapse ($\alpha\to0$) for all states at large $N$.

