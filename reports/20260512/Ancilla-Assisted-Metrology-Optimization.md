# Ancilla-Assisted Metrology: Full-parameter Optimization via Nelder–Mead

## 🧪 Hypothesis

For a system–ancilla pair of single-particle two-mode bosonic systems, the sensitivity $\Delta\omega$ (**error-propagation** uncertainty in estimating the rate parameter $\omega$) can be improved beyond the single-system SQL by optimising the initial states, beam-splitter times, holding time, and interaction coefficients. Specifically, interaction terms of the form $J_i^S \otimes J_j^A$ ($i,j \in \{x,z\}$) should generate entanglement between S and A during the holding period, converting ancilla-assisted correlations into enhanced metrological sensitivity at readout.

The central hypothesis is that a **non-zero interaction Hamiltonian** with suitably chosen coefficients $\alpha_{xx}, \alpha_{xz}, \alpha_{zx}, \alpha_{zz}$ will yield a sensitivity $[\Delta\omega]_{\text{opt}}$ strictly better than the best achievable with decoupled S and A ($\alpha_{ij} = 0$), and that the optimal configuration is a **non-trivial function of the true $\omega$**.

## ⚛️ Theoretical Model

The total Hilbert space is $\mathcal{H}_{\text{tot}} = \mathcal{H}_S \otimes \mathcal{H}_A$, where each subsystem is a **two-mode bosonic Fock space** truncated at one particle per mode. The single-particle sector $\mathcal{H}_{1} = \text{span}\{\vert1,0\rangle,\, \vert0,1\rangle\}$ (dimension 2) is isomorphic to a spin-$1/2$, and the full space has dimension 4 with ordered basis $\{\vert1,0\rangle_S\vert1,0\rangle_A,\, \vert1,0\rangle_S\vert0,1\rangle_A,\, \vert0,1\rangle_S\vert1,0\rangle_A,\, \vert0,1\rangle_S\vert0,1\rangle_A\}$. The **angular momentum operators** for each subsystem in this basis are $J_z = \frac12 \sigma_z$ and $J_x = \frac12 \sigma_x$ ($2\times2$ matrices), which satisfy the SU(2) algebra $[J_x, J_z] = i J_y$ with $J_y = \sigma_y/2$. These are embedded into the full space via Kronecker products: $J_z^S = J_z \otimes \mathbb{1}_2$, $J_x^S = J_x \otimes \mathbb{1}_2$, $J_z^A = \mathbb{1}_2 \otimes J_z$, $J_x^A = \mathbb{1}_2 \otimes J_x$. The **initial state** is a pure product state $\vert\Psi_0\rangle = \vert\psi_S\rangle \otimes \vert\psi_A\rangle$, where each single-particle state is parameterised as $\vert\psi\rangle = \cos(\theta/2)\,\vert1,0\rangle + e^{i\phi}\sin(\theta/2)\,\vert0,1\rangle$ with $\theta \in [0,\pi]$ and $\phi \in [0,2\pi)$, giving 4 degrees of freedom.

The **circuit** proceeds in three steps. First, a **beam-splitter unitary** generated by $J_x$ acts independently on each qubit: $U_{\text{BS}}(T) = \exp(-i T J_x) = \begin{pmatrix}\cos\frac{T}{2} & -i\sin\frac{T}{2}\\ -i\sin\frac{T}{2} & \cos\frac{T}{2}\end{pmatrix}$, applied as $U_{\text{BS}}^{(2)}(T) = U_{\text{BS}}(T) \otimes U_{\text{BS}}(T)$ with independent times $T_{\text{BS}1}$ (before the hold) and $T_{\text{BS}2}$ (after the hold). A 50/50 splitter corresponds to $T = \pi/2$. Second, during the **holding time** $T_H$, the system evolves under $H_{\text{hold}} = \omega\,(J_z^S \otimes \mathbb{1}_A) + H_{\text{int}}$, where $H_{\text{int}} = \alpha_{xx} J_x^S \otimes J_x^A + \alpha_{xz} J_x^S \otimes J_z^A + \alpha_{zx} J_z^S \otimes J_x^A + \alpha_{zz} J_z^S \otimes J_z^A$. Crucially, **only the system S accumulates phase from $\omega$**; the ancilla A experiences no $\omega$-dependent evolution and serves solely as a resource for entanglement via $H_{\text{int}}$. The hold unitary is $U_{\text{hold}}(T_H) = \exp(-i T_H H_{\text{hold}})$. Third, the second beam splitter is applied. The complete evolution is $\vert\Psi_{\text{final}}\rangle = U_{\text{BS}}^{(2)}(T_{\text{BS}2})\, U_{\text{hold}}(T_H)\, U_{\text{BS}}^{(2)}(T_{\text{BS}1})\, (\vert\psi_S\rangle \otimes \vert\psi_A\rangle)$.

The **measurement** observable is $J_z^S$ (system only). For a pure final state, the expectation value is $\langle J_z^S \rangle = \langle\Psi_{\text{final}}\vert J_z^S \vert\Psi_{\text{final}}\rangle$ and the variance is $\text{Var}(J_z^S) = \langle\Psi_{\text{final}}\vert (J_z^S)^2 \vert\Psi_{\text{final}}\rangle - \langle J_z^S \rangle^2$. The **sensitivity** via error propagation is $\Delta\omega = \sqrt{\text{Var}(J_z^S)} / \vert\partial\langle J_z^S\rangle/\partial\omega\vert$, where the derivative is computed via central finite differences with step $\delta = 10^{-6}$. In the decoupled baseline ($\alpha_{ij} = 0$), S and A evolve independently and the optimal sensitivity is $\Delta\omega = 1/T_H$ (the SQL for a single spin-$1/2$), achieved with $\vert\psi_S\rangle = \vert1,0\rangle$ and $T_{\text{BS}1} = T_{\text{BS}2} = \pi/2$. The ancilla provides no benefit in the decoupled regime, but the interaction $H_{\text{int}}$ is hypothesised to generate entanglement during the hold that could, in principle, improve sensitivity beyond $1/T_H$.

## 💻 Numerical Simulation

### Implementation Strategy

1. **Operator construction** — Build $J_z^S$, $J_z^A$, $J_x^S$, $J_x^A$ as $4\times4$ matrices in the two-qubit basis; no Fock-space truncation is needed because the single-particle subspace is exact.
2. **State preparation** — Parameterise each initial qubit state by $(\theta, \phi)$ on the Bloch sphere; the full state is the Kronecker product of the two single-qubit states.
3. **Beam-splitter unitaries** — Compute analytically as $U_{\text{BS}}(T) \in \mathbb{C}^{2\times2}$; the combined unitary is their Kronecker product, with independent times $T_{\text{BS}1}$ (before hold) and $T_{\text{BS}2}$ (after hold).
4. **Hold unitary** — Compute $U_{\text{hold}}(T_H) = \exp(-i T_H H_{\text{hold}})$ via `scipy.linalg.expm`, fast and exact for $4\times4$ matrices.
5. **Expectation values and derivative** — Compute $\langle J_z^S\rangle$ and $\text{Var}(J_z^S)$ via vector-matrix-vector products; obtain $\partial\langle J_z^S\rangle/\partial\omega$ via central finite differences with $\delta = 10^{-6}$, re-evaluating the full circuit at $\omega \pm \delta$.
6. **Optimisation** — Use `scipy.optimize.minimize(method='Nelder-Mead')` on the objective $f = \Delta\omega$ (to minimise). The parameter vector contains 11 entries: $\theta_S, \phi_S, \theta_A, \phi_A, T_{\text{BS}1}, T_{\text{BS}2}, T_H, \alpha_{xx}, \alpha_{xz}, \alpha_{zx}, \alpha_{zz}$. Enforce bounds via a penalty method; run Nelder–Mead from **10 random initial seeds** per $\omega$ value and retain the best result.

### Parameter Sweep

| Parameter | Value / Range |
|---|---|
| $\omega$ grid | 0.1, 0.2, 0.5, 1.0, 2.0, 5.0 |
| Restarts per $\omega$ | 10 (random seeds) |
| $\theta_S, \theta_A$ | $[0, \pi]$ |
| $\phi_S, \phi_A$ | $[0, 2\pi)$ |
| $T_{\text{BS}1}, T_{\text{BS}2}$ | $[0, \pi]$ |
| $T_H$ | $[0, 5]$ (default), expanded to $[0, 20]$ |
| $\alpha_{ij}$ | $[-2, 2]$ |
| Nelder–Mead maxiter | 5000 |
| $\alpha$ grid scan range | $[-2, 2]$, 21 points per coefficient, plus 4D random search (200 samples) |
| Finite-difference step $\delta$ | $10^{-6}$ |

### Validation

```python
assert np.isclose(np.linalg.norm(initial_state), 1.0), "Initial state normalised"
assert np.allclose(U_bs @ U_bs.conj().T, np.eye(2)), "BS unitary"
assert np.allclose(U_hold @ U_hold.conj().T, np.eye(4)), "Hold unitary"
assert np.isclose(np.linalg.norm(final_state), 1.0), "Final state normalised"
assert Var_jz >= -1e-12, "Variance must be non-negative"
assert delta_omega > 0, "Sensitivity must be positive"
```

#### 🔧 Implementation Status

| Component | Description |
|-----------|-------------|
| **Operator construction** | Pauli matrices, $J_z$, $J_x$, $J_y$ as $4\times4$ Kronecker products |
| **Interaction Hamiltonian** | $H_{\text{int}} = \sum_{ij} \alpha_{ij} J_i^S \otimes J_j^A$ |
| **State preparation** | Single- and two-qubit Bloch-sphere parameterisation |
| **Beam-splitter unitaries** | $U_{\text{BS}}(T) = \exp(-i T J_x)$ with finite-time $T$ |
| **Holding unitary** | $\exp(-i T_H [\omega J_z^S + H_{\text{int}}])$ |
| **Full circuit evolution** | BS1 $\to$ Hold $\to$ BS2, with normalisation checks |
| **Sensitivity** | $\Delta\omega = \sqrt{\text{Var}(J_z^S)} / \vert\partial\langle J_z^S\rangle/\partial\omega\vert$ via central finite differences |
| **Nelder–Mead wrapper** | `scipy.optimize.minimize(method='Nelder-Mead')` with 11 parameters |
| **$\omega$-scan runner** | Multiple restarts per $\omega$ value, all results recorded |
| **Validation helpers** | Hermiticity, unitarity, commutation, SQL baseline checks, derivative stability |
| **Bounds management** | Configurable bounds passed to optimisation (e.g., $T_H$ range changeable for expanded-bound investigations) |
| **Convergence history** | Objective values tracked via Nelder–Mead callback |
| **$\alpha$-scan: single parameter** | Scan one $\alpha_{ij}$, others fixed at zero |
| **$\alpha$-scan: random search** | Uniform sampling in 4D $\alpha$ space |
| **Interactive page** | Streamlit UI with sidebar controls, results table, $\Delta\omega$ vs $\omega$ plot, parameter visualisation, and single-configuration manual probe |

Test suite: **137 tests**, all passing.

## ⚠️ Expected Failure Conditions

| Failure | Description | Mitigation |
|---------|-------------|------------|
| Boundary saturation of $T_H$ | The optimiser consistently drives $T_H$ to its upper bound because longer holding times always improve the SQL-limited sensitivity ($\Delta\omega_{\text{SQL}} = 1/T_H$). The optimal $T_H$ is unbounded within this model. | Accept the saturation as a feature of the SQL; expand the $T_H$ bound to verify the scaling law continues ($\Delta\omega = 1/T_H$ holds at $T_H = 20$ and beyond). Do not interpret $T_H$ at the bound as an optimal trade-off. |
| Interaction coefficients driven to zero | The optimiser consistently converges to $\alpha_{ij} \approx 0$ because any non-zero interaction degrades sensitivity. The landscape at $\alpha=0$ is a global minimum. | Accept the result; verify analytically (via **QFI** bounds) that ancilla cannot help with S-only measurement. Use grid scans to confirm that $\alpha_{ij} \neq 0$ always degrades $\Delta\omega$. |
| Flat objective across restarts | Many different initial parameter sets converge to $\Delta\omega \approx 1/T_H$ because the landscape has a large plateau at the SQL when $\alpha \approx 0$ and $T_H$ is large. This causes high convergence spread across restarts. | Increase the number of restarts to verify the best result is stable; report the spread as a diagnostic of landscape flatness rather than local minima. |
| Nelder–Mead stagnation near analytical seed | When seeded with the analytically optimal decoupled solution ($\theta_S=0$, 50/50 BS, $\alpha=0$, $T_H=\pi$), the optimiser barely deviates, confirming the SQL solution is a strong attractor. | Use seeded runs as a sanity check; rely on random restarts for exploration. |
| No entanglement generated | The reduced-system purity remains $1.0$ throughout optimisation. The optimiser never finds entangled solutions beneficial, confirming entanglement with an unmeasured ancilla cannot improve S-only sensitivity. | Accept the negative result; verify analytically that the QFI bound precludes any entanglement-enabled improvement. |

## 🔬 Results

### $\omega$-Scan (Nelder–Mead Optimisation)

The Nelder–Mead optimisation was run over $\omega \in \{0.1, 0.2, 0.5, 1.0, 2.0, 5.0\}$ with 10 random restarts per $\omega$, maxiter $= 5000$.

| $\omega$ | Best $\Delta\omega$ | SQL ($1/T_H^*$) | vs SQL | Spread | $T_H^*$ | Purity | Entangled? |
|----------|--------------------|-----------------|--------|--------|---------|--------|------------|
| 0.1 | 0.200000 | 0.200000 | 0.00% | 0.915 | 5.000 | 1.000 | FAIL |
| 0.2 | 0.200000 | 0.200000 | 0.00% | 0.831 | 5.000 | 1.000 | FAIL |
| 0.5 | 0.200000 | 0.200000 | 0.00% | 0.391 | 5.000 | 1.000 | FAIL |
| 1.0 | 0.200000 | 0.200000 | 0.00% | 1.145 | 5.000 | 1.000 | FAIL |
| 2.0 | 0.200000 | 0.200000 | 0.00% | 0.158 | 5.000 | 1.000 | FAIL |
| 5.0 | 0.200053 | 0.200000 | -0.03% | 0.806 | 5.000 | 1.000 | FAIL |

**Key Finding**: The central hypothesis is **falsified** for this specific setup. $T_H$ **always saturates the upper bound** (5.0). When the bound was expanded to 20.0, $T_H$ saturated at 20.0 with $\Delta\omega = 0.05 = 1/20$, confirming the optimiser simply maximises $T_H$ to minimise $\Delta\omega$. **All interaction coefficients converge to $\alpha_{ij} \approx 0$** — the optimiser finds the decoupled solution in every case. **Purity is always 1.0**, confirming no entanglement is generated between S and A. **High convergence spread** ($>15\%$) across restarts reflects the flat landscape near the SQL, not true local minima.

### Grid Scan Over Interaction Coefficients

A systematic scan of each $\alpha_{ij}$ in $[-2, 2]$ with all other parameters fixed at the SQL-optimal values ($\theta_S=\pi/2$, $\phi_S=0$, $\theta_A=0$, $T_{\text{BS}1}=T_{\text{BS}2}=\pi/2$, $T_H=1.0$, $\omega_{\text{true}}=1.0$) shows that **$\Delta\omega(\alpha_{ij}) \geq 1/T_H$ for every $\alpha_{ij} \neq 0$** across all four interaction coefficients. The sensitivity is exactly $1.0$ at $\alpha_{ij}=0$ and increases monotonically with $\vert\alpha_{ij}\vert$. A random search over 200 points in the full 4D $\alpha$ space found **no combination achieving $\Delta\omega < 1/T_H$**. This conclusively demonstrates that any non-zero system–ancilla interaction degrades the sensitivity when $J_z^S$ is the sole measurement operator.

### Expanded-Bound Investigation

When the $T_H$ bound was expanded to 20.0, the best result was $\Delta\omega = 0.050000 = 1/20.0$, again at $T_H=20$ (boundary) and $\alpha=0$. The sensitivity scales exactly as $\Delta\omega = 1/T_H$, confirming SQL-limited behaviour regardless of the available $T_H$ range.

## ✅ Success Criteria

| # | Check | Expectation | Status |
|---|-------|-------------|--------|
| 1 | Nelder–Mead converges consistently across restarts | Spread $< 10\%$ | FAIL — Spread $>15\%$, but due to landscape flatness at SQL, not local minima |
| 2 | $\Delta\omega_{\text{opt}} \leq 1/T_H$ (decoupled SQL) | Achieves at least decoupled bound | PASS — $\Delta\omega_{\text{opt}} = 1/T_H$ exactly for all probed $\omega$ |
| 3 | $\Delta\omega_{\text{opt}} < 1/T_H$ for some $\omega$ (interaction helps) | At least one $\omega$ shows $> 5\%$ improvement | FAIL — **No improvement found.** Best $\Delta\omega$ is exactly $1/T_H$ for all $\omega$; grid scans confirm $\alpha_{ij} \neq 0$ always degrades sensitivity |
| 4 | All unitaries are unitary ($U^\dagger U = I$) | Assertions pass | PASS |
| 5 | State normalisation preserved | $\|\vert\Psi\rangle\| = 1$ | PASS |
| 6 | $\Delta\omega > 0$ for all probed $\omega$ | Positive and finite | PASS |
| 7 | Optimal parameters vary smoothly with $\omega$ | No discontinuous jumps | FAIL — High variance across restarts, but best-per-$\omega$ always converges to $T_H=5$ (boundary), $\alpha=0$ |
| 8 | At $\alpha=0$, recovers analytical solution | $\Delta\omega \approx 1/T_H$ | PASS |

Criteria 3 (the core hypothesis) is conclusively falsified: the interaction provides **no** sensitivity improvement for this setup. Criteria 2, 4, 5, 6, and 8 pass, confirming the implementation is correct and the negative result is physical, not numerical. The failure of Criteria 1 and 7 reflects landscape flatness rather than algorithmic deficiency.

#### ⚖️ Analytical Bounds

The numerical result is explained by a fundamental **Quantum Fisher Information (QFI)** bound. For estimation of $\omega$ imprinted via $U = \exp(-i \omega T_H J_z^S)$ on the system qubit, the QFI for a pure single-qubit state $\vert\psi_S\rangle$ is $F_Q(\vert\psi_S\rangle) = T_H^2 [1 - \langle J_z\rangle^2] \leq T_H^2$, giving $\Delta\omega \geq 1/T_H$ — the SQL. When the system is entangled with an ancilla and only S is measured, the reduced state is $\rho_S = \text{Tr}_A(\vert\Psi\rangle\langle\Psi\vert_{SA})$. By convexity of the QFI: $F_Q(\rho_S) \leq F_Q(\vert\Psi\rangle_{SA}) \leq F_Q(\vert\psi_S\rangle) \leq T_H^2$, so $\Delta\omega \geq 1/T_H$ regardless of entanglement, interaction strength, or measurement choice on S only. This bound is **unconditional**: the ancilla cannot help because partial trace is a convex operation, the ancilla does not couple to $\omega$, and the maximum QFI for a single qubit under $J_z$ rotation is $T_H^2$. Any non-zero $\alpha_{ij}$ only reduces the QFI further by creating a mixed reduced state, increasing $\Delta\omega$.

**Implications**: An ancilla can only help through **joint measurements** on S+A (e.g., $\langle J_z^S + J_z^A\rangle$ achieving the $N=2$ SQL: $\Delta\omega = 1/(2T_H)$) or via **interaction-based readout** that maps ancilla correlations back onto S before measurement (Davis et al., PRA 2016). The current protocol (BS1 → hold → BS2 → measure $J_z^S$) is equivalent to a single-qubit **MZI** — the ancilla is irrelevant for sensitivity.

## 🏁 Conclusions

**Key Finding**: The central hypothesis is **falsified** for the specific setup of $N=1$ per subsystem with $J_z^S$-only measurement. Non-zero interaction coefficients $\alpha_{ij}$ **never improve** the sensitivity $\Delta\omega$ below the SQL of $1/T_H$. Instead, any non-zero interaction strictly degrades performance. The root cause is a fundamental QFI bound: for any measurement performed solely on the system qubit (partial trace over ancilla), the Quantum Fisher Information cannot exceed $T_H^2$, giving $\Delta\omega \geq 1/T_H$ — independent of the interaction Hamiltonian, the ancilla state, or the beam-splitter settings.

**What the optimiser does find**: It correctly identifies that longer holding times $T_H$ give better sensitivity ($\Delta\omega_{\text{SQL}} = 1/T_H$), that $\alpha_{ij} = 0$ is optimal (any interaction is detrimental), and converges to the SQL exactly, validating the correctness of the implementation. **What the optimiser cannot find** (because it does not exist within this model): any configuration achieving $\Delta\omega < 1/T_H$ or any configuration where entanglement improves the $S$-only measurement.

**Open items**: (a) Does a **joint measurement** on S+A (e.g., $J_z^S + J_z^A$, parity measurement) unlock sensitivity enhancement beyond the $N=1$ SQL, corresponding to $N=2$ Heisenberg scaling? (b) Can **interaction-based readout** (an additional interaction period after the hold, before the second BS) map ancilla correlations back onto S, enabling enhancement with an S-only measurement? (c) What happens with **multiple particles** ($N > 1$ per subsystem), where spin-squeezing and two-axis counter-twisting can generate metrologically useful entanglement? (d) Would a **different estimation strategy** (e.g., Bayesian estimation or maximum-likelihood) recover sensitivity when operating at fringe extrema, where the error-propagation formula diverges?
